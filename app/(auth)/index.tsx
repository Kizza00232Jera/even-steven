import { View, Text } from 'react-native';

export default function SignIn() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-text-primary font-display text-2xl">Sign In</Text>
    </View>
  );
}
